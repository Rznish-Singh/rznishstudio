NEXT_PUBLIC_SITE_URL=https://www.rznishstudio.vercel.app
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=

# Optional — set this to enable Cloudinary-optimized delivery (auto AVIF/WebP,
# responsive `sizes`, on-the-fly crops) via the CldImage component. Leave unset
# to keep using plain https CDN URLs everywhere (default, no account needed).
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=
